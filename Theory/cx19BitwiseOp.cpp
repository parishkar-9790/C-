#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vx;
typedef vector<char> chvx;
typedef vector<long long> llvx;
#define input(x) cin >> x;
#define output(x) cout << x << endl;
#define all(v) v.begin(), v.end()
#define sorts(v) sort(all(v))
#define reverses(v) reverse(all(v))
#define fp(i, x, n) for (long long i = x; i < n; i++)
#define fn(i, x, n) for (long long i = x; n < i; i--)
int main()
{
    ll t;
    input(t);
    while (t--)
    {
    }
    return 0;
}