#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vx;
typedef vector<char> chvx;
typedef vector<long long> llvx;
#define all(v) v.begin(), v.end()
#define sorts(v) sort(all(v))
#define reverses(v) reverse(all(v))
int main()
{
    // bitwise AND& operator
    // it take two bits at a time and perform and operation
    // it take two numbers and perform bitweise and
    // result of annd is one when both the bits are 1
    //   0 1 1 1 --->7
    // & 0 1 0 0 --->4
    //   0 1 0 0 --->4

    // bitwise or operator
    //   0 1 1 1 --->7
    // | 0 1 0 0 --->4
    //   0 1 1 1 --->7
    
    // bitwise not operator
    // not is unary operator
    //   0 1 1 1 --->7
    // ~ 1 0 0 0 --->8

    return 0;
}